# Transformer Pipeline

This directory contains all the original source code for the experiments in this project. It includes the scripts for data preprocessing, the definitions of the transformer models, and the main script for training and evaluating the models.

## Directory Structure

```
Transformer_Pipeline/
├── Preprocessing/
│   └── Load_Data.py
│   └── Cyber_Trend_to_Graph.py
└── Cyber_Trend_Graph_Dataset.py
└── Models/
    └── example_model.py
```
## Preprocessing Scripts

The Preprocessing subdirectory contains the scripts for loading and transforming the raw cyber threat data into formats suitable for the transformer models.

* **`Preprocessing/Load_Data.py`**:
    * Provides a reusable function (`load_cyber_threat_data`) to load the raw `Cyber_Trend_Forecasting_All.csv` file.
    * Handles date parsing and sets the 'Date' column as the DataFrame index.
    * Performs basic validation (checking file existence, sorting data).

* **`Preprocessing/Cyber_Trend_to_Graph.py`**:
    * Imports the loading function from `Preprocessing/Load_Data.py`.
    * Contains the main pipeline steps to convert the loaded time-series DataFrame into a graph-based format suitable for spatio-temporal graph models.
    * Responsibilities include: chronological splitting, graph structure definition (adjacency matrix), data normalisation, creating time-series windows, and saving the final processed files (adjacency matrix, windowed data, scaler).

## Dataset Script

The Cyber_Trend_Graph_Dataset.py script contains the core PyTorch Dataset class for our project. The class linkw between the processed data files (generated by the Preprocessing/ scripts) and the DataLoader that feeds the model during training.

* **`Cyber_Trend_Graph_Dataset.py`**
    * **Purpose:** Defines a custom PyTorch `Dataset` class to interface between our processed files and the PyTorch `DataLoader` for the training loop.
    * **Process:**
        * Defines a class (e.G., `CyberThreatGraphDataset`) that inherits from `torch.utils.data.Dataset`.
        * `__init__`: Loads the pre-processed, windowed data (e.g., `train.npz`) for a specific split (train, val, or test).
        * `__len__`: Returns the total number of samples (windows).
        * `__getitem__`: Retrieves a single sample (`X`, `y`) by its index.
        * `get_static_features()`: A helper method that loads and returns a dictionary of all the other static artifacts (`adj_mx`, `dtw_matrix`, etc.) needed by the model's constructor.

## Model Scripts

The Model subdirectory contains model architecture files, i.e the wrapper for PDFormer.

* **`Models/Cyber_Trend_Train.py`**
* The main script to run the experiment. It creates the `Dataset` and `DataLoader`, initialises the `Model`, and runs the training and evaluation loops.

## How to Run the Pipeline

Follow these steps to preprocess the data and run the training experiments.

1.  **Install Dependencies**
    ```bash
    ...
    ```

2.  **Run Preprocessing**
    ```bash
    ...
    ```

3.  **Run Training**
    ```bash
    ...
    ```

## Dependencies

All Python libraries required to run the code in this pipeline are listed in the `requirements.txt` file at the root of the main project repository. Please ensure you have installed them before running any scripts.
